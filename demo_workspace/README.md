# Calculator demo

This deliberately small project contains a defect. Run the tests, locate the cause,
make the smallest correct fix, run the tests again, and summarize the result.

Test command:

```bash
python -m unittest -v
```

